#include <iostream.h>
#include <conio.h>

//variabel perulangan
int x, y, z, n, o, p;

main(){

	cout << "PERULANGAN 2" << endl;
   cout << "Masukkan input anda : ";
	cin >> z;

   for(x=1,y=0,z=0,n=1; z<p; z++){
    	if (z % 2 == 0){
       	cout << x << " ";
         x = x + (8 * o);
         n++;
      }
      else if(z % 2 == 1){
      	y = y + 4;
         cout << y << " ";
      }
   }
   getch();
}